<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Light">
        <!-- Customize your theme here. -->
    </style>

    <!-- 标题 -->
    <style name="title_text">
        <item name="android:textSize">@dimen/font_18</item>
        <item name="android:textColor">@color/text_write</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:layout_centerHorizontal">true</item>
    </style>

    <!-- 未填写TextView -->
    <style name="did_text">
        <item name="android:textSize">14sp</item>
        <item name="android:singleLine">true</item>
        <item name="android:layout_marginLeft">@dimen/x30</item>
        <item name="android:textColor">@color/text_darkgrey</item>
    </style>

    <!-- 未填写EditView -->
    <style name="did_edit">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/text_somber</item>
        <item name="android:singleLine">true</item>
        <item name="android:paddingRight">@dimen/x30</item>
        <item name="android:maxLength">20</item>
        <item name="android:textColorHint">@color/text_hint</item>
        <item name="android:background">@null</item>
    </style>
    <!-- 333333 -->
    <style name="text_333">
        <item name="android:textSize">16sp</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColor">@color/text_somber</item>
    </style>
    <!-- 666666 -->
    <style name="text_666">
        <item name="android:textSize">14sp</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColor">@color/text_darkgrey</item>
    </style>
    <!-- 999999 -->
    <style name="text_999">
        <item name="android:textSize">14sp</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColor">@color/text_lightgray</item>
    </style>
    <!-- cccccc -->
    <style name="text_ccc">
        <item name="android:textSize">14sp</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColor">@color/text_hint</item>
    </style>
    <!-- 提交按钮 -->
    <style name="submit_btn">
        <item name="android:textSize">16sp</item>
        <item name="android:layout_marginLeft">@dimen/x30</item>
        <item name="android:background">@drawable/bg_submit</item>
        <item name="android:layout_marginRight">@dimen/x30</item>
        <item name="android:textColor">@color/btn_hint</item>
    </style>
    <!-- LinearLayout_item -->
    <style name="linear_item">
        <item name="android:gravity">center_vertical</item>
        <item name="android:background">@color/text_write</item>
        <item name="android:orientation">horizontal</item>
    </style>
    <!-- 提交按钮 -->
    <style name="submit_bg">
        <item name="android:textSize">16sp</item>
        <item name="android:layout_marginLeft">@dimen/x30</item>
        <item name="android:background">@drawable/bg_submit</item>
        <item name="android:layout_marginRight">@dimen/x30</item>
        <item name="android:textColor">@color/btn_hint</item>
    </style>
    <!-- 验证码按钮 -->
    <style name="auth_btn">
        <item name="android:textSize">14sp</item>
        <item name="android:background">@drawable/bg_submit</item>
        <item name="android:textColor">@color/btn_hint</item>
    </style>
    <!-- 审核不通过提示 -->
    <style name="lose_text">
        <item name="android:textSize">16sp</item>
        <item name="android:background">@color/base_lightgray</item>
        <item name="android:paddingLeft">@dimen/x30</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@color/text_red</item>
    </style>

    <!-- 自定义仿IOS的AlertDialog的样式 -->
    <style name="AlertDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>
